version: "2"
services:
    app:
        build: ./app
        env_file:
            -   '.env'
        links:
            - db
        ports:
            - "40001:40001"

    db:
        image: mysql:8.0.26
        ports:
            - "32000:3306"
        environment:
            MYSQL_ROOT_PASSWORD: root
        volumes:
            - ./db:/docker-entrypoint-initdb.d/:ro/